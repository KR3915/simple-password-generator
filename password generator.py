import random
from string import ascii_uppercase
def generate_chars(size, type):
    chars = "".join(random.choice(chars) for i in range(size)
generate_chars(2, string.ascii_uppercase + string.digits)
    